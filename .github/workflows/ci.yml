name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows MSVC
            os: windows-latest
            preset: windows-msvc-release
            qt_arch: win64_msvc2022_64
          - name: Linux GCC
            os: ubuntu-latest
            preset: linux-release
            qt_arch: linux_gcc_64
          - name: macOS AppleClang
            os: macos-latest
            preset: macos-release
            qt_arch: clang_64

    steps:
      - uses: actions/checkout@v4

      - name: Install Linux system packages
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            libegl1 \
            libfontconfig1-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-0 \
            libxcb-cursor0 \
            libxcb-icccm4 \
            libxcb-image0 \
            libxcb-keysyms1 \
            libxcb-randr0 \
            libxcb-render-util0 \
            libxcb-shape0 \
            libxcb-xinerama0 \
            libxcb-xkb1

      - name: Install CMake and Ninja
        uses: lukka/get-cmake@latest

      - name: Set up MSVC environment
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      # 4.4.x uses the "$/action" self-repository syntax, which the Windows runner does not accept yet.
      # aqtinstall 3.3.0 predates the flattened Qt 6.11 Windows repository layout; the pinned commit
      # is the merge of miurahr/aqtinstall#1000 that adds support for it.
      - name: Install Qt
        uses: jurplel/install-qt-action@v4.3.1
        with:
          version: 6.11.2
          arch: ${{ matrix.qt_arch }}
          modules: qtmultimedia
          cache: true
          aqtsource: git+https://github.com/miurahr/aqtinstall.git@8c3695d4a4e1ceabf6a74dc6c79681656dc6b74b

      - name: Configure
        run: cmake --preset ${{ matrix.preset }}
        env:
          QT_ROOT: ${{ env.QT_ROOT_DIR }}

      - name: Build
        run: cmake --build --preset ${{ matrix.preset }}

      - name: Test
        run: ctest --preset ${{ matrix.preset }} --output-on-failure --no-tests=error
